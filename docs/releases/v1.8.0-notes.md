# v1.8.0 Release Notes (2026-07-20)

> **状态**：本地已构建（annotated tag `v1.8.0` 已创建，**未推送**）。
> **GitHub Release / PyPI 上传**：需用户**显式**授权，本 notes 文本供 T2-C 复用。

---

## 摘要

Bootstrap Phase 1 推进：artifact 重建 + 版本治理。本版本**不**引入新的
ontology 概念、不变更 PSA 契约、不变更包格式。所有动作都围绕"已有资产
如何被正确宣告与分发"，为 Level 3 readiness 铺路。

## 包含变更

- **`packages/transformer-0.1.0/`** 与新增的 `packages/core-0.1.0/`、
  `packages/equipment-0.1.0/` 三个 PSA package 在 v1.8.0 同步重建
  （`scripts/build_psa_package.py --all`），共 40 个 artifact 文件；
  语义与 v1.7.0 完全一致，仅 `manifest.generated_at` 时间戳更新
- **CHANGELOG.md** `[1.8.0]` 段（按 Keep a Changelog 1.1 五段格式）
- **`docs/releases/v1.8.0-notes.md`**：本文件
- **`docs/superpowers/plans/2026-07-20-level-3-bootstrap.md`**：
  Level 3 Bootstrap Plan v0.1（4 阶段：T2-A / T2-B / T2-C / T3-A）
- **`docs/tooling-companions/cim-ontology-toolkit.md`** 版本兼容表更新
- **`docs/governance/cts-gap-register.md`** A1-A4 closed 状态同步

## 兼容性

- **API**：与 v1.7.0 完全兼容（无 ontology 概念变更）
- **PSA Semantic Package**：`transformer-0.1.0` 与之前版本字节级等价
  （如出现差异，仅 IR `generated_at` 时间戳；语义不变）
- **依赖**：与 v1.7.0 一致

## 已知 GAP（不影响 v1.8.0）

| ID | 描述 | 优先级 | Target |
|----|------|--------|--------|
| GAP-005 | EASG Runtime 真实加载未验证 | P2 | 2026-08-15 |
| GAP-006 | PSA 官方 CTS 规范未发布 | P2 | TBD |
| GAP-007 | SCR-Metadata 映射未验证 | P3 | — |

## 升级建议

无需任何迁移操作——v1.8.0 是分发流程推进，不修改消费者接口。

## 致谢

- cim-ontology-toolkit 团队（cross-reference 维护）
- PSA主仓 `tooling/README.md` 维护者
- EASG Runtime 团队（A5 进度跟踪）

## 完整变更

参见 `CHANGELOG.md` `[1.8.0]` 段。

---

## 部署注意事项

- 本 notes 不作为 Release 上传触发器；T2-C（GitHub Release）需用户
  显式授权才执行（per Plan §G6）
- 本地 tag `v1.8.0` 已创建但**未推送**；push 触发同样需要用户授权
- `maturity.psa_level: 2` 保持不变；Level 3 声明需 T3-A 全部 gate ✅